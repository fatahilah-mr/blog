---
import Base from '../layouts/Base.astro';


// This acts as a global layout wrapper with Header and Footer included
interface Props {
  title: string;
  description: string;
}
const { title, description } = Astro.props;
---
<Base title={title} description={description}>
  <slot />
</Base>
